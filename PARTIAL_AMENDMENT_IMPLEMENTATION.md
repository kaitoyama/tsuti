# 部分改正（Partial Amendment / Scoped Apply）実装まとめ

## 実装概要

実際の厚生労働省の保医発等のPDFは、文書全体ではなく**特定のセクション（サブツリー）だけ**の新旧対照表を公表することがあります（例：「第２の６」だけを改正）。この部分的な改正に対応するため、`--partial` フラグを実装しました。

## 主な変更

### 1. コア機能 (`shinkyu.py`)

#### `apply_children` 関数
- 新しい `partial` パラメータを追加
- `partial=True` の場合：
  - 対照表に出現するラベルのセットを事前抽出
  - 項目が見つからない場合、前の項目をスキップして探索
  - 対照表に出てこない項目は原本のまま保持
  - 末尾の検証（`strict`）を無効化

#### `cmd_apply` 関数
- `partial` パラメータを `apply_children` に渡す
- `base_version` の検証は `--partial` の有無に関わらず実行（`--force` で回避可能）

### 2. CLI

```bash
python shinkyu.py apply BASE.tsuchi.txt PATCH.shinkyu.txt -o NEW.tsuchi.txt --partial
```

新しいフラグ：
- `--partial`: 部分改正モード。対照表に出てこない項目は原本のまま残す

### 3. 動作の違い

| 項目 | 通常モード | 部分改正モード (`--partial`) |
|------|-----------|---------------------------|
| 対照表に出てこない項目 | エラー | 原本のまま保持 |
| `base_version` 検証 | 必須 | **必須**（`--force` で回避可能） |
| 全項目出現の検証 | 必須 | スキップ |
| 項目の順序 | 厳密に一致必須 | 柔軟に探索 |
| 参照追従 | 有効 | **有効** ✓ |

## テスト

### テスト1: 基本的な部分改正 (`tests/test_partial_apply.py`)

**シナリオ:**
- 原本に第１、第２（サービスＡ・Ｂ・Ｃ）、第３が存在
- 対照表は第２のサービスＡとＢのみを改正

**確認項目:**
- ✓ `--partial` なしでは失敗する
- ✓ `--partial` ありで成功する
- ✓ 対照表に出てこない項目（第１、第２の３、第３）が保持される
- ✓ 対照表に出てくる項目（第２の１、第２の２）が改正される
- ✓ 新設項目が正しく追加される

### テスト2: 参照追従 (`tests/test_partial_refs.py`)

**シナリオ:**
- サービスＡで②の前に新設 → ③・④に繰下げ
- サービスＢ・Ｃは対照表に出てこないが、サービスＡを参照

**確認項目:**
- ✓ サービスＡの繰下げが正しく適用される
- ✓ 対照表に出てこないサービスＢの参照が追従（１②→１③）
- ✓ 対照表に出てこないサービスＣの参照が追従（１③→１④）
- ✓ 対照表に出てこないセクション（第２）が保持される

### テスト3: 後方互換性

既存のサンプルファイル（`examples/sample*.txt`）で完全適用モードが正常に動作することを確認。

## 使用例

### 例1: 基本的な部分改正

```bash
# 第２の１だけを改正する部分的な新旧対照表を適用
python shinkyu.py apply R7.tsuchi.txt 部分改正_第２の１のみ.shinkyu.txt \
    -o R8.tsuchi.txt --partial
```

### 例2: 参照追従を含む完成版を出力

```bash
# 部分改正を適用し、参照追従を含めた完成版の対照表テキストも出力
python shinkyu.py apply R7.tsuchi.txt 部分改正.shinkyu.txt \
    -o R8.tsuchi.txt --partial \
    --patch-out 部分改正_完成版.shinkyu.txt
```

## 実装の詳細

### アルゴリズム

1. **事前準備（`partial=True` の場合のみ）**
   - 対照表に出現するラベルのセットを抽出
   - 範囲の（略）も展開してセットに含める

2. **適用処理**
   - 対照表の項目を順に処理
   - 項目が見つからない場合：
     - 通常モード: エラー
     - 部分改正モード: 前の項目をスキップして探索
       - スキップされた項目が対照表に出てこない場合、原本のまま保持
   - 項目が見つかった場合: 通常の適用処理を実行

3. **末尾処理**
   - 通常モード: 残った項目があればエラー（`strict=True` の場合）
   - 部分改正モード: 残った項目を原本のまま保持

### 参照追従との統合

部分改正モードでも、参照追従機能は正常に動作します：

1. `R.mark_uids(broot)` で原本の各項目にUIDをマーク
2. `R.snapshot(broot)` で改正前の参照先を解決
3. `apply_children(..., partial=True)` で部分改正を適用
4. `R.follow(broot, new, snap)` で参照を追従

部分改正で保持された項目（`copy.deepcopy` でコピー）も、元のUIDを保持しているため、参照追従の対象になります。

## 制約事項

1. **Q&A / 事務連絡には影響なし**
   - 本実装は通知文書（`.tsuchi.txt`）の適用処理のみに影響
   - Q&A / 事務連絡のパーサーには一切変更なし

2. **実データでの検証は別途**
   - 本PRではフィクスチャベースのテストのみ実施
   - 実際の保医発PDFでの検証は、データ取得後に別途実施予定

3. **デフォルトは完全適用モード**
   - 明示的に `--partial` を指定しない限り、従来通りの動作
   - 後方互換性を完全に維持

## 今後の拡張可能性

1. **自動検出モード**
   - メタデータ（`scope` フィールド等）から自動的に部分改正を判定
   - 例: `scope: "第２の６"` が指定されていれば自動的に `--partial` 相当の動作

2. **範囲指定**
   - `--partial-scope "第２の６"` のような明示的な範囲指定
   - より厳密な検証が可能

3. **統計情報**
   - どのセクションが改正されたか/保持されたかのサマリー出力
   - 差分の可視化

## 関連ファイル

- `shinkyu.py`: コア実装
- `README.md`: ドキュメント
- `tests/test_partial_apply.py`: 基本テスト
- `tests/test_partial_refs.py`: 参照追従テスト
- `tests/run_all_tests.sh`: 統合テストランナー
- `tests/fixtures/partial_*.txt`: テスト用フィクスチャ

## PR

https://github.com/kaitoyama/tsuti/pull/3
